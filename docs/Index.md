
##入门
~~~  
use IopenQQ\Core\App;

$data = [
           'client_id' => 'client_id',
           'client_secret' => 'client_secret',
           'redirect_uri' => 'redirect_uri'
        ]
$app = $app = new App($data);
~~~

>$app 是所有操作的实例。
